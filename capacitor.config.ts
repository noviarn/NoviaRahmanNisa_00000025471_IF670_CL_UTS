import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'uts_mobile2_25471',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
